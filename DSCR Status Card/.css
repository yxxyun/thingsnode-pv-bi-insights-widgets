/* ════════════════════════════════════════════════════
   DSCR Status Card
   ThingsBoard v4.3.0 PE | Latest Values
   Dynamic scaling — all sizes in em
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & SHARED TOKENS
   ───────────────────────────── */
.dscr-card {
    /* Accent — default cyan */
    --c-cyan: #06F5FF;
    --c-cyan-glow: rgba(6, 245, 255, 0.15);
    --c-cyan-border: rgba(6, 245, 255, 0.55);

    /* Severity Colors */
    --c-good: #66BB6A;
    --c-good-glow: rgba(102, 187, 106, 0.2);
    --c-critical: #FF5252;
    --c-critical-glow: rgba(255, 82, 82, 0.25);

    /* Current State (dynamic via JS) */
    --c-accent: var(--c-cyan);
    --c-glow: var(--c-cyan-glow);

    /* Background */
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);

    /* Text hierarchy */
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;

    /* Surfaces */
    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    /* ─── Layout: Absolute fill ─── */
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;
    justify-content: center;

    background: var(--c-bg);
    border: 1px solid var(--c-accent);
    box-shadow: 0 0 12px var(--c-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.5em 0.7em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.4s ease, box-shadow 0.4s ease;
}

/* ─── Status Modes ─── */
.dscr-card.status-good {
    --c-accent: var(--c-good);
    --c-glow: var(--c-good-glow);
}

.dscr-card.status-critical {
    --c-accent: var(--c-critical);
    --c-glow: var(--c-critical-glow);
    animation: dscr-pulse-border 2s infinite;
}

/* ─── Hover Glow ─── */
.dscr-card:hover {
    box-shadow: 0 0 22px var(--c-glow), inset 0 0 8px rgba(0, 0, 0, 0.25);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1;
    margin-bottom: 0.15em;
    min-height: 0;
}

.card-title {
    font-size: 0.7em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
}

.header-right {
    display: flex;
    align-items: center;
    gap: 0.2em;
    flex-shrink: 0;
    padding-left: 0.4em;
}

/* ─── Status Pill ─── */
.status-pill {
    display: flex;
    align-items: center;
    gap: 0.3em;
    background: rgba(255, 255, 255, 0.08);
    padding: 0.2em 0.5em;
    border-radius: 1em;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.status-dot {
    width: 0.35em;
    height: 0.35em;
    border-radius: 50%;
    background-color: var(--c-text-muted);
    box-shadow: 0 0 4px rgba(0, 0, 0, 0.5);
    transition: background-color 0.3s ease;
}

.dscr-card.status-good .status-dot {
    background-color: var(--c-good);
    box-shadow: 0 0 6px var(--c-good);
}

.dscr-card.status-critical .status-dot {
    background-color: var(--c-critical);
    box-shadow: 0 0 6px var(--c-critical);
}

.status-text {
    font-size: 0.38em;
    font-weight: 600;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.04em;
}

/* ─────────────────────────────
   BODY
   ───────────────────────────── */
.card-body {
    flex: 1 1 auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    /* Center main value */
    z-index: 2;
    min-height: 0;
}

.value-row {
    display: flex;
    align-items: baseline;
    gap: 0.1em;
}

.main-value {
    font-size: 2.2em;
    font-weight: 700;
    color: var(--c-accent);
    line-height: 1;
    text-shadow: 0 0 10px var(--c-glow);
    transition: color 0.3s ease;
}

.unit {
    font-size: 0.9em;
    font-weight: 600;
    color: var(--c-accent);
    opacity: 0.9;
}

/* ─────────────────────────────
   FOOTER
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    z-index: 2;
    margin-top: 0.2em;
    padding-top: 0.2em;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    display: flex;
    justify-content: center;
    /* Center footer */
    gap: 0.4em;
    min-height: 0;
}

.footer-label {
    font-size: 0.45em;
    font-weight: 500;
    color: var(--c-text-dim);
    opacity: 0.8;
}

.footer-value {
    font-size: 0.45em;
    font-weight: 700;
    color: var(--c-text);
}

/* ─────────────────────────────
   TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4em 0.6em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.dscr-card:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.5em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   ANIMATIONS
   ───────────────────────────── */
.skeleton {
    opacity: 0.5;
    animation: dscr-pulse-load 1.5s infinite;
}

@keyframes dscr-pulse-load {
    0% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.6;
    }

    100% {
        opacity: 0.3;
    }
}

@keyframes dscr-pulse-border {
    0% {
        box-shadow: 0 0 5px rgba(255, 82, 82, 0.4);
    }

    50% {
        box-shadow: 0 0 15px rgba(255, 82, 82, 0.7);
    }

    100% {
        box-shadow: 0 0 5px rgba(255, 82, 82, 0.4);
    }
}