.curtailment-card {
    --color-bg: rgba(2, 10, 67, 0.9);
    --color-text-primary: #FFFFFF;
    --color-text-secondary: #B0BEC5;
    --color-cyan: #06F5FF;
    --color-red-fill: rgba(229, 57, 53, 0.4);
    --color-potential: rgba(255, 255, 255, 0.7);
    
    --font-family: 'Roboto', sans-serif;

    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100%;
    background-color: var(--color-bg);
    border-radius: 6px;
    padding: 16px;
    box-sizing: border-box;
    font-family: var(--font-family);
    color: var(--color-text-primary);
    overflow: hidden;
    position: relative;
}

.chart-header {
    text-align: center;
    margin-bottom: 10px;
    flex: 0 0 auto;
}

.chart-title {
    font-size: 14px;
    font-weight: 700;
    color: var(--color-text-primary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

.chart-container {
    flex: 1;
    position: relative;
    width: 100%;
    height: 100%; 
    min-height: 0;
}

/* Legend Overlay */
.custom-legend {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-top: 5px;
    font-size: 11px;
    color: var(--color-text-secondary);
}

.legend-item { display: flex; align-items: center; gap: 6px; }
.dot { width: 10px; height: 3px; border-radius: 2px; }
.dot.pot { background-color: var(--color-potential); border: 1px dashed #fff; }
.dot.act { background-color: var(--color-cyan); }
.dot.curt { background-color: rgba(229, 57, 53, 0.8); height: 8px; }