/* ════════════════════════════════════════════════════
   Curtailment vs Potential Power — V3
   ThingsBoard v4.2.1.1 PE | Latest Values Widget
   Envelope chart: Potential | Exported | Curtailed
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & TOKENS
   ───────────────────────────── */
.curt-card {
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.8);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.25);

    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;

    --c-good: #66BB6A;
    --c-warning: #FFC107;
    --c-critical: #FF5252;

    --c-cyan: #06F5FF;
    --c-red-fill: rgba(229, 57, 53, 0.4);
    --c-cyan-fill: rgba(6, 245, 255, 0.2);
    --c-potential: rgba(255, 255, 255, 0.5);

    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.35em 0.5em 0.25em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

.curt-card:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 22px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1.1;
    margin-bottom: 0.1em;
}

.card-title {
    font-size: 0.65em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    text-align: center;
}

.header-status {
    display: flex;
    align-items: center;
    gap: 0.18em;
    flex-shrink: 0;
    padding-left: 0.3em;
}

.status-dot {
    width: 0.35em;
    height: 0.35em;
    border-radius: 50%;
    background-color: var(--c-text-muted);
    display: inline-block;
    transition: background-color 0.4s ease, box-shadow 0.4s ease;
}

.status-dot.live {
    background-color: var(--c-good);
    box-shadow: 0 0 5px rgba(102, 187, 106, 0.6);
}

.status-dot.simulated {
    background-color: var(--c-warning);
    box-shadow: 0 0 5px rgba(255, 193, 7, 0.6);
}

.status-dot.nodata {
    background-color: var(--c-text-muted);
    box-shadow: none;
    opacity: 0.5;
}

.status-text {
    font-size: 0.38em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
}

/* ─────────────────────────────
   CHART BODY
   ───────────────────────────── */
.chart-body {
    flex: 1 1 0;
    display: flex;
    align-items: stretch;
    min-height: 0;
    position: relative;
    z-index: 2;
    gap: 0.15em;
}

.y-axis-title {
    writing-mode: vertical-rl;
    transform: rotate(180deg);
    font-size: 0.38em;
    font-weight: 600;
    color: var(--c-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.chart-container {
    flex: 1;
    position: relative;
    min-height: 0;
    min-width: 0;
}

.chart-container canvas {
    width: 100% !important;
    height: 100% !important;
}

/* ─────────────────────────────
   FOOTER — LEGEND
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.8em;
    padding-top: 0.15em;
    z-index: 2;
}

.legend-item {
    display: flex;
    align-items: center;
    gap: 0.2em;
}

.legend-label {
    font-size: 0.38em;
    font-weight: 500;
    color: var(--c-text-dim);
    white-space: nowrap;
}

.legend-line {
    width: 0.8em;
    height: 0;
    border-top-width: 2px;
    border-top-style: solid;
}

.legend-line.dashed {
    border-top-style: dashed;
    border-top-color: var(--c-potential);
}

.legend-line.solid {
    border-top-style: solid;
    border-top-color: var(--c-cyan);
}

.legend-block {
    width: 0.6em;
    height: 0.35em;
    border-radius: 1px;
}

.legend-block.curtailed {
    background-color: rgba(229, 57, 53, 0.7);
}

/* ─────────────────────────────
   TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4em 0.55em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.curt-card:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.45em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   SKELETON & ANIMATIONS
   ───────────────────────────── */
.skeleton {
    opacity: 0.5;
    animation: curt-pulse-load 1.5s infinite;
}

@keyframes curt-pulse-load {
    0% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.6;
    }

    100% {
        opacity: 0.3;
    }
}