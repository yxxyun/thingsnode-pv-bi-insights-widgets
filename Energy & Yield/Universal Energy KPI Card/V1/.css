/* ========================================
   DESIGN SYSTEM - Energy KPI Card
   Style: Sri Lanka PV Project / Cyan Glow
   ======================================== */
.energy-card {
    /* --- COLORS --- */
    --c-bg-gradient: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);
    --c-border: #1373ef; /* Blue-ish border from screenshot */
    --c-glow: rgba(6, 245, 255, 0.15); /* Soft Cyan Glow */
    --c-value: #06F5FF; /* High contrast Cyan */
    --c-title: #FFFFFF;
    --c-subtitle: #90A4AE;
    --c-unit: #06F5FF;

    /* --- TYPOGRAPHY --- */
    --f-family: 'Roboto', sans-serif;
    --f-title: 0.8em;
    --f-value: 2.5em;
    --f-unit: 1.1em;
    --f-sub: 0.75em;

    /* --- LAYOUT --- */
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    
    background: var(--c-bg-gradient);
    border: 1px solid rgba(19, 115, 239, 0.4);
    box-shadow: 0 0 15px var(--c-glow), inset 0 0 20px rgba(0,0,0,0.5);
    border-radius: 8px;
    padding: 10px;
    box-sizing: border-box;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
}

.energy-card:hover {
    border-color: rgba(6, 245, 255, 0.8);
    box-shadow: 0 0 25px rgba(6, 245, 255, 0.25), inset 0 0 10px rgba(0,0,0,0.2);
}

/* --- SECTIONS --- */
.card-header {
    flex: 0 0 auto;
    margin-bottom: 5px;
    text-align: center;
    width: 100%;
    z-index: 2;
}

.card-title {
    font-family: var(--f-family);
    font-size: var(--f-title);
    font-weight: 700;
    color: var(--c-title);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-shadow: 0 2px 4px rgba(0,0,0,0.8);
}

.card-body {
    flex: 1;
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 6px; /* Space between value and unit */
    width: 100%;
    z-index: 2;
}

.metric-value {
    font-family: var(--f-family);
    font-size: var(--f-value);
    font-weight: 700;
    color: var(--c-value);
    line-height: 1;
    text-shadow: 0 0 10px rgba(6, 245, 255, 0.4);
}

.metric-unit {
    font-family: var(--f-family);
    font-size: var(--f-unit);
    font-weight: 600;
    color: var(--c-unit);
    opacity: 0.9;
}

.card-footer {
    flex: 0 0 auto;
    margin-top: 5px;
    text-align: center;
    width: 100%;
    z-index: 2;
}

.card-subtitle {
    font-family: var(--f-family);
    font-size: var(--f-sub);
    font-weight: 400;
    color: var(--c-subtitle);
}

/* --- LOADING STATE --- */
.skeleton {
    opacity: 0.3;
    animation: pulse 1.5s infinite;
}

@keyframes pulse {
    0% { opacity: 0.3; }
    50% { opacity: 0.6; }
    100% { opacity: 0.3; }
}