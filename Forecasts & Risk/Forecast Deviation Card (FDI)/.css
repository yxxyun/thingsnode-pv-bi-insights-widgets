/* ════════════════════════════════════════════════════
   Forecast Deviation Card (FDI) — v2.2
   ThingsBoard v4.3.0 PE | Latest Values
   Compact horizontal layout — optimized for ~500×125px
   Dynamic scaling — all sizes in em
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & TOKENS
   ───────────────────────────── */
.fdi-card {
    /* Accent — cyan default */
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.7);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.22);

    /* Background */
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);

    /* Text hierarchy */
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;

    /* Semantic */
    --c-good: #66BB6A;
    --c-warning: #FFC107;
    --c-critical: #FF5252;

    /* Surfaces */
    --c-track: rgba(255, 255, 255, 0.07);
    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    /* Layout */
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.4em 0.7em 0.3em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

.fdi-card:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 22px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ═════════════════════════════
   SEVERITY ACCENT OVERRIDES
   ═════════════════════════════ */
.fdi-card.sev-good {
    --c-accent: #66BB6A;
    --c-accent-glow: rgba(102, 187, 106, 0.12);
    --c-accent-border: rgba(102, 187, 106, 0.35);
    --c-accent-hover: rgba(102, 187, 106, 0.65);
    --c-accent-glow-hover: rgba(102, 187, 106, 0.22);
}

.fdi-card.sev-warning {
    --c-accent: #FFC107;
    --c-accent-glow: rgba(255, 193, 7, 0.12);
    --c-accent-border: rgba(255, 193, 7, 0.35);
    --c-accent-hover: rgba(255, 193, 7, 0.65);
    --c-accent-glow-hover: rgba(255, 193, 7, 0.22);
}

.fdi-card.sev-critical {
    --c-accent: #FF5252;
    --c-accent-glow: rgba(255, 82, 82, 0.12);
    --c-accent-border: rgba(255, 82, 82, 0.35);
    --c-accent-hover: rgba(255, 82, 82, 0.65);
    --c-accent-glow-hover: rgba(255, 82, 82, 0.22);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1;
    margin-bottom: 0.15em;
    min-height: 0;
}

.card-title {
    font-size: 0.7em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    min-width: 0;
}

.header-right {
    display: flex;
    align-items: center;
    gap: 0.25em;
    flex-shrink: 0;
    padding-left: 0.4em;
}

/* ─── Status Dot (Header) ─── */
.status-dot {
    width: 0.45em;
    height: 0.45em;
    border-radius: 50%;
    background-color: var(--c-text-muted);
    display: inline-block;
    transition: background-color 0.4s ease, box-shadow 0.4s ease;
}

.status-dot.good {
    background-color: var(--c-good);
    box-shadow: 0 0 6px rgba(102, 187, 106, 0.7);
}

.status-dot.warning {
    background-color: var(--c-warning);
    box-shadow: 0 0 6px rgba(255, 193, 7, 0.7);
}

.status-dot.critical {
    background-color: var(--c-critical);
    box-shadow: 0 0 6px rgba(255, 82, 82, 0.7);
    animation: fdi-pulse-critical 1.8s infinite;
}

.status-text {
    font-size: 0.48em;
    font-weight: 700;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
    transition: color 0.3s ease;
}

.status-text.good {
    color: var(--c-good);
}

.status-text.warning {
    color: var(--c-warning);
}

.status-text.critical {
    color: var(--c-critical);
}

/* ─── Resolution Label ─── */
.resolution-label {
    font-size: 0.42em;
    font-weight: 700;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: rgba(255, 255, 255, 0.06);
    padding: 0.15em 0.5em;
    border-radius: 3px;
    white-space: nowrap;
}

/* ─────────────────────────────
   BODY — 2 columns with separator
   ───────────────────────────── */
.card-body {
    flex: 1 1 auto;
    display: flex;
    align-items: center;
    gap: 0.8em;
    z-index: 2;
    min-height: 0;
}

/* ─── Left: Value Block — dominant ─── */
.value-block {
    flex: 1 1 auto;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
}

.dev-value {
    font-size: 2.2em;
    font-weight: 700;
    color: var(--c-accent);
    line-height: 1;
    text-shadow: 0 0 12px var(--c-accent-glow), 0 0 25px rgba(6, 245, 255, 0.18);
    white-space: nowrap;
    transition: color 0.3s ease, text-shadow 0.3s ease;
}

.dev-label {
    font-size: 0.42em;
    font-weight: 600;
    color: var(--c-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin-top: 0.1em;
}

/* ─── Vertical Separator ─── */
.body-sep {
    width: 1px;
    align-self: stretch;
    margin: 0.3em 0;
    background: linear-gradient(180deg,
            transparent 0%,
            rgba(255, 255, 255, 0.15) 30%,
            rgba(255, 255, 255, 0.15) 70%,
            transparent 100%);
    flex-shrink: 0;
}

/* ─── Right: Context Values — larger ─── */
.ctx-block {
    flex: 0 0 auto;
    display: flex;
    flex-direction: column;
    gap: 0.3em;
    min-width: 5em;
}

.ctx-row {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.4em;
}

.ctx-label {
    font-size: 0.45em;
    font-weight: 600;
    color: var(--c-text-muted);
    text-transform: uppercase;
    white-space: nowrap;
    letter-spacing: 0.03em;
}

.ctx-val {
    font-size: 0.65em;
    font-weight: 700;
    color: var(--c-text);
    white-space: nowrap;
    text-align: right;
    text-shadow: 0 0 6px rgba(255, 255, 255, 0.06);
}

/* ─────────────────────────────
   FOOTER — simplified
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: flex-end;
    z-index: 2;
    margin-top: 0.1em;
    padding-top: 0.1em;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    min-height: 0;
}

.footer-text {
    font-size: 0.42em;
    font-weight: 500;
    color: var(--c-text-muted);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    opacity: 0.6;
    text-align: right;
}

/* ─────────────────────────────
   TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.35em 0.5em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.fdi-card:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.45em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   SKELETON & ANIMATIONS
   ───────────────────────────── */
.skeleton {
    opacity: 0.5;
    animation: fdi-pulse-load 1.5s infinite;
}

@keyframes fdi-pulse-load {
    0% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.6;
    }

    100% {
        opacity: 0.3;
    }
}

@keyframes fdi-pulse-critical {
    0% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }

    50% {
        box-shadow: 0 0 10px rgba(255, 82, 82, 0.9);
    }

    100% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }
}