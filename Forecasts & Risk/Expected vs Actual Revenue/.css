/* ════════════════════════════════════════════════════
   Expected vs Actual Revenue
   ThingsBoard v4.3.0 PE | Time Series Bar Chart
   Dynamic scaling — all sizes in em
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & TOKENS
   ───────────────────────────── */
.rev-card {
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.8);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.25);

    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;

    --c-good: #66BB6A;
    --c-critical: #EF5350;

    --c-series-a: #5C6BC0;
    --c-series-b: #00E5FF;

    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.4em 0.5em 0.3em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

.rev-card:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 22px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1.1;
    margin-bottom: 0.1em;
}

.card-title {
    font-size: 0.58em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    min-width: 0;
}

.header-status {
    display: flex;
    align-items: center;
    gap: 0.18em;
    flex-shrink: 0;
    padding-left: 0.3em;
}

.status-dot {
    width: 0.38em;
    height: 0.38em;
    border-radius: 50%;
    background-color: var(--c-text-muted);
    display: inline-block;
    transition: background-color 0.4s ease, box-shadow 0.4s ease;
}

.status-dot.good {
    background-color: var(--c-good);
    box-shadow: 0 0 5px rgba(102, 187, 106, 0.6);
}

.status-dot.neutral {
    background-color: #90A4AE;
    box-shadow: 0 0 4px rgba(144, 164, 174, 0.4);
}

.status-dot.critical {
    background-color: var(--c-critical);
    box-shadow: 0 0 5px rgba(239, 83, 80, 0.6);
    animation: pulse-critical 1.8s infinite;
}

.status-text {
    font-size: 0.42em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
}

/* ─────────────────────────────
   BODY — CHART AREA
   ───────────────────────────── */
.chart-body {
    flex: 1;
    position: relative;
    display: flex;
    min-height: 0;
    /* left: y-axis title + grid labels | bottom: x-axis labels | top: badge headroom */
    padding: 0.6em 0.15em 1.1em 2.4em;
    z-index: 2;
}

/* Y-axis title (rotated) */
.y-axis-title {
    position: absolute;
    left: 0.25em;
    top: 50%;
    transform: rotate(-90deg) translateX(-50%);
    transform-origin: center left;
    font-size: 0.42em;
    font-weight: 600;
    color: var(--c-text-muted);
    white-space: nowrap;
    letter-spacing: 0.03em;
}

/* Chart area — grid and bars overlap */
.chart-area {
    flex: 1;
    position: relative;
    min-height: 0;
    min-width: 0;
}

/* Grid lines — absolute behind bars */
.grid-lines {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    pointer-events: none;
    z-index: 1;
}

.grid-line {
    position: relative;
    width: 100%;
    border-top: 1px dashed rgba(255, 255, 255, 0.12);
}

.grid-zero {
    border-top: none;
    border-bottom: 1px solid rgba(255, 255, 255, 0.2);
}

.grid-label {
    position: absolute;
    right: 100%;
    top: -0.35em;
    padding-right: 0.25em;
    font-size: 0.38em;
    font-weight: 700;
    color: var(--c-text-muted);
    white-space: nowrap;
}

/* Bars container — absolute over grid */
.bars-container {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    display: flex;
    align-items: flex-end;
    justify-content: space-around;
    z-index: 2;
}

/* ─────────────────────────────
   BAR GROUPS (JS-generated)
   ───────────────────────────── */
.bar-group {
    flex: 1;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    position: relative;
    max-width: 16%;
}

.bars-wrapper {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    gap: 0.06em;
}

.bar {
    width: 45%;
    position: relative;
    border-radius: 0.1em 0.1em 0 0;
    transition: height 0.6s cubic-bezier(0.4, 0, 0.2, 1), filter 0.2s ease;
    min-height: 1px;
}

.bar::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(to bottom, rgba(255, 255, 255, 0.12), rgba(0, 0, 0, 0.05));
    border-radius: inherit;
    pointer-events: none;
}

.bar:hover {
    filter: brightness(1.25);
}

.bar-a {
    background-color: var(--c-series-a);
}

.bar-b {
    background-color: var(--c-series-b);
}

/* Value labels on top of bars */
.bar-value-label {
    position: absolute;
    bottom: 100%;
    left: 50%;
    transform: translateX(-50%);
    margin-bottom: 0.06em;
    font-size: 0.32em;
    font-weight: 600;
    color: var(--c-text);
    white-space: nowrap;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.6);
}

/* X-axis month labels */
.x-axis-label {
    position: absolute;
    bottom: -1.4em;
    left: 50%;
    transform: translateX(-50%);
    font-size: 0.36em;
    font-weight: 700;
    color: var(--c-text-dim);
    text-transform: uppercase;
    white-space: nowrap;
}

/* Variance badge */
.variance-badge {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    font-size: 0.36em;
    font-weight: 700;
    white-space: nowrap;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    z-index: 10;
}

.variance-badge.positive {
    color: var(--c-good);
}

.variance-badge.negative {
    color: var(--c-critical);
}

/* ─────────────────────────────
   FOOTER — LEGEND
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    display: flex;
    justify-content: center;
    gap: 1em;
    padding-top: 0.15em;
    z-index: 2;
}

.legend-item {
    display: flex;
    align-items: center;
    gap: 0.2em;
}

.legend-dot {
    width: 0.38em;
    height: 0.38em;
    border-radius: 0.06em;
    background-color: #555;
    flex-shrink: 0;
}

.legend-label {
    font-size: 0.42em;
    font-weight: 500;
    color: var(--c-text-dim);
    white-space: nowrap;
}

/* ─────────────────────────────
   TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4em 0.55em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.rev-card:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.5em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   SKELETON & ANIMATIONS
   ───────────────────────────── */
.skeleton {
    opacity: 0.5;
    animation: pulse-load 1.5s infinite;
}

@keyframes pulse-load {
    0% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.6;
    }

    100% {
        opacity: 0.3;
    }
}

@keyframes pulse-critical {
    0% {
        box-shadow: 0 0 4px rgba(239, 83, 80, 0.6);
    }

    50% {
        box-shadow: 0 0 10px rgba(239, 83, 80, 0.9);
    }

    100% {
        box-shadow: 0 0 4px rgba(239, 83, 80, 0.6);
    }
}