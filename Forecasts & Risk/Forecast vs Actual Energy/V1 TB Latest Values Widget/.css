/* ════════════════════════════════════════════════════
   Forecast vs. Actual Energy — V1
   ThingsBoard v4.2.1.1 PE | Latest Values Widget
   Probabilistic forecast envelope (P50/P75/P90)
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & TOKENS
   ───────────────────────────── */
.fvae-card {
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.8);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.25);

    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;

    --c-good: #66BB6A;
    --c-warning: #FFC107;
    --c-critical: #FF5252;

    --c-actual: #06F5FF;
    --c-p50: #448AFF;
    --c-p75: #FFC107;
    --c-p90: #FF5252;
    --c-band: rgba(68, 138, 255, 0.12);

    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.35em 0.5em 0.25em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

.fvae-card:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 22px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1.1;
    margin-bottom: 0.1em;
}

.card-title {
    font-size: 0.65em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    text-align: center;
}

.header-status {
    display: flex;
    align-items: center;
    gap: 0.18em;
    flex-shrink: 0;
    padding-left: 0.3em;
}

.status-dot {
    width: 0.35em;
    height: 0.35em;
    border-radius: 50%;
    background-color: var(--c-text-muted);
    display: inline-block;
    transition: background-color 0.4s ease, box-shadow 0.4s ease;
}

.status-dot.good {
    background-color: var(--c-good);
    box-shadow: 0 0 5px rgba(102, 187, 106, 0.6);
}

.status-dot.warning {
    background-color: var(--c-warning);
    box-shadow: 0 0 5px rgba(255, 193, 7, 0.6);
}

.status-dot.critical {
    background-color: var(--c-critical);
    box-shadow: 0 0 5px rgba(255, 82, 82, 0.6);
    animation: fvae-pulse-critical 1.8s infinite;
}

.status-dot.nodata {
    background-color: var(--c-text-muted);
    box-shadow: none;
    opacity: 0.5;
}

.status-dot.derived {
    background-color: var(--c-p50);
    box-shadow: 0 0 5px rgba(68, 138, 255, 0.6);
}

.status-dot.simulated {
    background-color: var(--c-warning);
    box-shadow: 0 0 5px rgba(255, 193, 7, 0.4);
}

.status-text {
    font-size: 0.38em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
}

/* ─────────────────────────────
   CHART BODY
   ───────────────────────────── */
.chart-body {
    flex: 1 1 0;
    display: flex;
    align-items: stretch;
    min-height: 0;
    position: relative;
    z-index: 2;
    gap: 0.15em;
}

.y-axis-title {
    writing-mode: vertical-rl;
    transform: rotate(180deg);
    font-size: 0.38em;
    font-weight: 600;
    color: var(--c-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.chart-container {
    flex: 1;
    position: relative;
    min-height: 0;
    min-width: 0;
}

.chart-container canvas {
    width: 100% !important;
    height: 100% !important;
}

/* ─────────────────────────────
   FOOTER — LEGEND
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.6em;
    padding-top: 0.15em;
    z-index: 2;
    flex-wrap: wrap;
}

.legend-item {
    display: flex;
    align-items: center;
    gap: 0.18em;
}

.legend-label {
    font-size: 0.35em;
    font-weight: 500;
    color: var(--c-text-dim);
    white-space: nowrap;
}

/* Legend swatches */
.legend-swatch {
    width: 0.7em;
    height: 0;
    border-top-width: 2px;
    border-top-style: solid;
}

.legend-swatch.line-actual {
    border-top-color: var(--c-actual);
    border-top-style: solid;
    border-top-width: 2.5px;
}

.legend-swatch.line-p50 {
    border-top-color: var(--c-p50);
    border-top-style: dashed;
}

.legend-swatch.line-p75 {
    border-top-color: var(--c-p75);
    border-top-style: dashed;
}

.legend-swatch.line-p90 {
    border-top-color: var(--c-p90);
    border-top-style: dashed;
}

.legend-swatch.band-fill {
    width: 0.6em;
    height: 0.35em;
    border-top: none;
    border-radius: 1px;
    background: linear-gradient(180deg,
            rgba(68, 138, 255, 0.25) 0%,
            rgba(255, 82, 82, 0.15) 100%);
}

/* ─────────────────────────────
   TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4em 0.55em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.fvae-card:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.45em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   ANIMATIONS
   ───────────────────────────── */
@keyframes fvae-pulse-critical {
    0% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }

    50% {
        box-shadow: 0 0 10px rgba(255, 82, 82, 0.9);
    }

    100% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }
}