/* ════════════════════════════════════════════════════
   Revenue-at-Risk Breakdown Widget
   ThingsBoard v4.3.0 PE | Latest Values
   Dynamic scaling — all sizes in em
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & SHARED TOKENS
   ───────────────────────────── */
.rar-card {
    /* Accent — coral-red default (revenue risk) */
    --c-accent: #FF7043;
    --c-accent-glow: rgba(255, 112, 67, 0.12);
    --c-accent-border: rgba(255, 112, 67, 0.4);
    --c-accent-hover: rgba(255, 112, 67, 0.7);
    --c-accent-glow-hover: rgba(255, 112, 67, 0.22);

    /* Segment Palette — category-family mapped (luminance-balanced) */
    --c-fault: #D32F2F;
    /* red — operational failure (dominant) */
    --c-soiling: #8D6E63;
    /* brown — environmental */
    --c-curtail: #E67E22;
    /* muted orange — grid/external */
    --c-degrad: #F0C040;
    /* deeper amber — asset aging */
    --c-elect: #78909C;
    /* blue-grey — system */
    --c-other: #607D8B;

    /* Background */
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);

    /* Text hierarchy */
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #A0B4BC;

    /* Semantic colors */
    --c-good: #66BB6A;
    --c-warning: #FFC107;
    --c-critical: #FF5252;

    /* Surfaces */
    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    /* ─── Layout: Absolute fill ─── */
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 8px var(--c-accent-glow), inset 0 0 12px rgba(0, 0, 0, 0.35);
    border-radius: 8px;
    padding: 0.5em 0.7em 0.4em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

/* ─── Hover Glow ─── */
.rar-card:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 8px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ═════════════════════════════
   SEVERITY ACCENT OVERRIDES
   ═════════════════════════════ */

.rar-card.sev-good {
    --c-accent: #66BB6A;
    --c-accent-glow: rgba(102, 187, 106, 0.12);
    --c-accent-border: rgba(102, 187, 106, 0.35);
    --c-accent-hover: rgba(102, 187, 106, 0.65);
    --c-accent-glow-hover: rgba(102, 187, 106, 0.22);
}

.rar-card.sev-warning {
    --c-accent: #FFC107;
    --c-accent-glow: rgba(255, 193, 7, 0.12);
    --c-accent-border: rgba(255, 193, 7, 0.35);
    --c-accent-hover: rgba(255, 193, 7, 0.65);
    --c-accent-glow-hover: rgba(255, 193, 7, 0.22);
}

.rar-card.sev-critical {
    --c-accent: #FF5252;
    --c-accent-glow: rgba(255, 82, 82, 0.12);
    --c-accent-border: rgba(255, 82, 82, 0.35);
    --c-accent-hover: rgba(255, 82, 82, 0.65);
    --c-accent-glow-hover: rgba(255, 82, 82, 0.22);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1;
    margin-bottom: 0.15em;
    min-height: 0;
}

.card-title {
    font-size: 0.75em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    min-width: 0;
}

.header-right {
    display: flex;
    align-items: center;
    gap: 0.3em;
    flex-shrink: 0;
    padding-left: 0.5em;
}

/* ─── Resolution Label (replaces severity badge) ─── */
.resolution-label {
    font-size: 0.55em;
    font-weight: 700;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: rgba(255, 255, 255, 0.06);
    padding: 0.1em 0.4em;
    border-radius: 3px;
    white-space: nowrap;
}

/* ─────────────────────────────
   BODY
   ───────────────────────────── */
.card-body {
    flex: 1 1 auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    z-index: 2;
    min-height: 0;
    gap: 0.3em;
}

/* Bar Container — wraps track + ticks */
.bar-container {
    position: relative;
}

/* The Stacked Bar Track */
.stacked-bar-track {
    display: flex;
    width: 100%;
    height: 2.8em;
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 4px;
    overflow: hidden;
    background: rgba(0, 0, 0, 0.2);
}

/* Percentage Tick Marks */
.bar-ticks {
    position: relative;
    height: 0.6em;
    margin-top: 0.1em;
}

.bar-tick {
    position: absolute;
    transform: translateX(-50%);
    font-size: 0.6em;
    font-weight: 600;
    color: var(--c-text-muted);
    opacity: 0.75;
    white-space: nowrap;
}

.bar-tick::before {
    content: '';
    position: absolute;
    top: -3px;
    left: 50%;
    transform: translateX(-50%);
    width: 1px;
    height: 3px;
    background: rgba(255, 255, 255, 0.25);
}

/* Dynamic Segments */
.segment {
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    transition: width 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    overflow: hidden;
    position: relative;
    border-right: 1px solid rgba(0, 0, 0, 0.15);
    cursor: default;
}

.segment:last-child {
    border-right: none;
}

.segment:hover {
    filter: brightness(1.15);
}

/* Color Classes */
.bg-fault {
    background-color: var(--c-fault);
}

.bg-soiling {
    background-color: var(--c-soiling);
}

.bg-curtail {
    background-color: var(--c-curtail);
}

.bg-degrad {
    background-color: var(--c-degrad);
}

.bg-elect {
    background-color: var(--c-elect);
}

.bg-other {
    background-color: var(--c-other);
}

/* Text Labels inside segments */
.seg-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 0 0.15em;
}

.seg-label {
    font-size: 0.65em;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.9);
    white-space: nowrap;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.6);
    line-height: 1.1;
}

.seg-val {
    font-size: 0.8em;
    font-weight: 700;
    color: #FFF;
    white-space: nowrap;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.6);
}

/* Total Row */
.total-row {
    display: flex;
    align-items: baseline;
    gap: 0.3em;
}

.total-label {
    font-size: 0.65em;
    font-weight: 500;
    color: var(--c-text-dim);
}

/* Legend removed — segments use title attributes for hover tooltips */

.total-val {
    font-size: 0.85em;
    font-weight: 700;
    color: #FFFFFF;
    text-shadow: 0 0 6px rgba(255, 255, 255, 0.15);
}

/* ─────────────────────────────
   FOOTER
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    z-index: 2;
    margin-top: 0.1em;
    padding-top: 0.1em;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    min-height: 0;
}

.footer-label {
    font-size: 0.6em;
    font-weight: 600;
    color: var(--c-text-dim);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    opacity: 0.8;
}

/* ─────────────────────────────
   TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4em 0.6em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.rar-card:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.55em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   LOADING SKELETON
   ───────────────────────────── */
.skeleton {
    opacity: 0.5;
    animation: rar-pulse-load 1.5s infinite;
    background-color: rgba(255, 255, 255, 0.1);
    color: transparent !important;
    border-radius: 4px;
}

@keyframes rar-pulse-load {
    0% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.6;
    }

    100% {
        opacity: 0.3;
    }
}

@keyframes rar-pulse-critical {
    0% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }

    50% {
        box-shadow: 0 0 10px rgba(255, 82, 82, 0.9);
    }

    100% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }
}

/* ═════════════════════════════════════════════════════
   WIDE MODE (aspect ratio > 3:1)
   Compress vertical stack — every element shrinks
   ═════════════════════════════════════════════════════ */

/* --- Header tighter --- */
.wide .card-header {
    margin-bottom: 0.05em;
}

.wide .card-title {
    font-size: 0.55em;
}

.wide .resolution-label {
    font-size: 0.42em;
}

/* --- Body: minimal gaps --- */
.wide .card-body {
    gap: 0.05em;
}

/* --- Bar: shorter but visible --- */
.wide .stacked-bar-track {
    height: 1.6em;
    min-height: 12px;
    flex-shrink: 0;
}

.wide .seg-label {
    font-size: 0.52em;
}

.wide .seg-val {
    font-size: 0.63em;
}

/* --- Bar ticks: compact in wide mode --- */
.wide .bar-ticks {
    height: 0.4em;
    margin-top: 0.05em;
}

.wide .bar-tick {
    font-size: 0.38em;
}

/* --- Total: smaller --- */
.wide .total-row {
    gap: 0.2em;
    margin-top: 0;
}

.wide .total-label {
    font-size: 0.5em;
}

.wide .total-val {
    font-size: 0.65em;
}

/* --- Footer: minimal --- */
.wide .card-footer {
    margin-top: 0.02em;
    padding-top: 0.02em;
}

.wide .footer-label {
    font-size: 0.45em;
}