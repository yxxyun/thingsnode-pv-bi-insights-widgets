/* ════════════════════════════════════════════════════
   Risk Summary Panel Widget
   ThingsBoard v4.3.0 PE
   Dynamic scaling — all sizes in em
   ════════════════════════════════════════════════════ */
.risk-panel {
    /* --- COLOR TOKENS --- */
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.7);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.22);

    --c-critical: #FF5252;
    --c-critical-glow: rgba(255, 82, 82, 0.25);
    --c-critical-border: rgba(255, 82, 82, 0.6);

    --c-warning: #FFC107;
    --c-warning-glow: rgba(255, 193, 7, 0.25);
    --c-warning-border: rgba(255, 193, 7, 0.6);

    --c-good: #66BB6A;
    --c-good-glow: rgba(102, 187, 106, 0.25);
    --c-good-border: rgba(102, 187, 106, 0.6);

    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #A0B4BC;
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);
    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    /* --- LAYOUT: Absolute fill --- */
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;
    justify-content: center;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 8px var(--c-accent-glow), inset 0 0 12px rgba(0, 0, 0, 0.35);
    border-radius: 8px;
    padding: 0.5em 0.7em 0.4em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

/* ─── Default Hover Glow ─── */
.risk-panel:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 8px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ═════════════════════════════
   STATE STYLES (Dynamic)
   ═════════════════════════════ */
.risk-panel.state-critical {
    border-color: var(--c-critical-border);
    box-shadow: 0 0 8px var(--c-critical-glow), inset 0 0 12px rgba(0, 0, 0, 0.35);
}

.risk-panel.state-critical:hover {
    border-color: #FF8A80;
    box-shadow: 0 0 12px var(--c-critical-glow), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

.risk-panel.state-warning {
    border-color: var(--c-warning-border);
    box-shadow: 0 0 8px var(--c-warning-glow), inset 0 0 12px rgba(0, 0, 0, 0.35);
}

.risk-panel.state-warning:hover {
    border-color: #FFD54F;
    box-shadow: 0 0 12px var(--c-warning-glow), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

.risk-panel.state-good {
    border-color: var(--c-good-border);
    box-shadow: 0 0 8px var(--c-good-glow), inset 0 0 12px rgba(0, 0, 0, 0.35);
}

.risk-panel.state-good:hover {
    border-color: #A5D6A7;
    box-shadow: 0 0 12px var(--c-good-glow), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.panel-header {
    flex: 0 0 auto;
    z-index: 2;
    line-height: 1;
    margin-bottom: 0.15em;
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-height: 0;
}

.panel-title {
    font-size: 0.75em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    min-width: 0;
}

.header-right {
    display: flex;
    align-items: center;
    gap: 0.3em;
    flex-shrink: 0;
    padding-left: 0.5em;
}

/* ─── Resolution Label (replaces severity badge) ─── */
.resolution-label {
    font-size: 0.55em;
    font-weight: 700;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: rgba(255, 255, 255, 0.06);
    padding: 0.1em 0.4em;
    border-radius: 3px;
    white-space: nowrap;
}

/* ─────────────────────────────
   CONTENT
   ───────────────────────────── */
.panel-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    z-index: 2;
    min-height: 0;
}

/* --- Value Row (RaR) --- */
.value-row {
    display: flex;
    align-items: baseline;
    justify-content: flex-start;
    gap: 0.2em;
    margin-bottom: 0.3em;
}

.main-value {
    font-size: 1.8em;
    font-weight: 700;
    color: var(--c-text);
    line-height: 1;
    text-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
}

.unit {
    font-size: 0.8em;
    font-weight: 600;
    color: var(--c-text-dim);
    opacity: 0.9;
}

/* --- Sub-Content --- */
.sub-content {
    min-height: 0;
    display: flex;
    flex-direction: column;
    gap: 0.25em;
}

/* Status Row */
.status-row {
    display: flex;
    align-items: center;
    font-size: 0.75em;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.03em;
}

.status-label {
    color: var(--c-text-dim);
    margin-right: 0.4em;
    font-weight: 500;
}

.status-value {
    color: var(--c-text);
}

/* Color overrides for status text */
.state-critical .status-value {
    color: var(--c-critical);
}

.state-warning .status-value {
    color: var(--c-warning);
}

.state-good .status-value {
    color: var(--c-good);
}

/* ─── Risk Gauge Bar ─── */
.risk-gauge {
    display: flex;
    align-items: center;
    gap: 0.3em;
}

.gauge-track {
    flex: 1;
    height: 0.35em;
    background: rgba(255, 255, 255, 0.08);
    border-radius: 0.2em;
    overflow: visible;
    position: relative;
}

.gauge-fill {
    height: 100%;
    border-radius: 0.2em;
    transition: width 0.6s cubic-bezier(0.4, 0, 0.2, 1), background 0.4s ease;
    width: 0%;
    background: var(--c-accent);
}

.state-critical .gauge-fill {
    background: linear-gradient(90deg, rgba(255, 82, 82, 0.75), rgba(255, 138, 128, 0.65));
}

.state-warning .gauge-fill {
    background: linear-gradient(90deg, rgba(255, 193, 7, 0.75), rgba(255, 213, 79, 0.65));
}

.state-good .gauge-fill {
    background: linear-gradient(90deg, rgba(102, 187, 106, 0.75), rgba(165, 214, 167, 0.65));
}

/* ─── Gauge Tick Markers (P50, P75, P90) ─── */
.gauge-tick {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 1px;
    background: rgba(255, 255, 255, 0.3);
    z-index: 2;
    pointer-events: none;
}

.tick-p50 {
    left: 50%;
}

.tick-p75 {
    left: 75%;
}

.tick-p90 {
    left: 90%;
}

.gauge-tick::after {
    content: attr(data-label);
    position: absolute;
    bottom: 100%;
    left: 50%;
    transform: translateX(-50%);
    font-size: 0.5em;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.7);
    white-space: nowrap;
    margin-bottom: 0.15em;
}

.gauge-label {
    font-size: 0.6em;
    font-weight: 700;
    color: var(--c-text-dim);
    white-space: nowrap;
    min-width: 2em;
    text-align: right;
}

/* Tracking Row */
.tracking-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 0.65em;
    color: var(--c-text-dim);
}

.tracking-container {
    display: flex;
    align-items: center;
}

.tracking-label {
    margin-right: 0.4em;
    display: none;
}

.tracking-value {
    color: #FFF;
    font-weight: 600;
}

/* Trend Icon */
.trend-icon {
    display: flex;
    align-items: center;
    justify-content: center;
}

.trend-icon svg {
    width: 1.2em;
    height: 1.2em;
    fill: currentColor;
}

.state-critical .trend-icon {
    color: var(--c-critical);
    transform: rotate(0deg);
}

.state-good .trend-icon {
    color: var(--c-good);
    transform: rotate(180deg);
}

.state-warning .trend-icon {
    color: var(--c-warning);
    transform: rotate(90deg);
}

/* ─────────────────────────────
   FOOTER
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    z-index: 2;
    margin-top: 0.1em;
    padding-top: 0.1em;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    min-height: 0;
}

.footer-label {
    font-size: 0.6em;
    font-weight: 600;
    color: var(--c-text-dim);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    opacity: 0.8;
}

/* ─────────────────────────────
   TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4em 0.6em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.risk-panel:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.55em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   LOADING SKELETON
   ───────────────────────────── */
.skeleton {
    opacity: 0.5;
    animation: risk-pulse-load 1.5s infinite;
    background-color: rgba(255, 255, 255, 0.1);
    color: transparent !important;
    border-radius: 4px;
}

@keyframes risk-pulse-load {
    0% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.6;
    }

    100% {
        opacity: 0.3;
    }
}

@keyframes risk-pulse-critical {
    0% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }

    50% {
        box-shadow: 0 0 10px rgba(255, 82, 82, 0.9);
    }

    100% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }
}

/* ═════════════════════════════════════════════════════
   WIDE MODE (aspect ratio > 3:1)
   Content shifts from vertical stack → horizontal row
   ═════════════════════════════════════════════════════ */

/* --- Layout: single content row --- */
.wide .panel-header {
    margin-bottom: 0.05em;
}

.wide .panel-content {
    flex-direction: row;
    align-items: center;
    gap: 0;
}

/* --- Value: smaller to share horizontal space --- */
.wide .value-row {
    flex-shrink: 0;
    margin-bottom: 0;
    padding-right: 0.5em;
    border-right: 1px solid rgba(255, 255, 255, 0.1);
}

.wide .main-value {
    font-size: 1.4em;
}

.wide .unit {
    font-size: 0.6em;
}

/* --- Sub-Content: row layout --- */
.wide .sub-content {
    flex: 1;
    flex-direction: row;
    align-items: center;
    gap: 0;
    min-width: 0;
}

/* --- Status: compact --- */
.wide .status-row {
    flex-shrink: 0;
    padding: 0 0.5em;
    border-right: 1px solid rgba(255, 255, 255, 0.1);
    font-size: 0.6em;
}

/* --- Gauge: fills available space --- */
.wide .risk-gauge {
    flex: 1;
    padding: 0 0.5em;
    min-width: 0;
}

.wide .gauge-track {
    height: 0.3em;
}

.wide .gauge-label {
    font-size: 0.45em;
}

/* --- Tracking: hidden in wide mode (gauge carries the data) --- */
.wide .tracking-row {
    display: none;
}

/* --- Footer: minimal margin --- */
.wide .card-footer {
    margin-top: 0.05em;
    padding-top: 0.05em;
}

.wide .footer-label {
    font-size: 0.5em;
}