/* ========================================
   Debt Service Panel
   ThingsBoard v4.3.0 PE | Latest Values
   Dynamic scaling — all sizes in em
   ======================================== */
.dscr-panel {
    /* --- COLOR TOKENS --- */
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.8);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.25);
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;
    --c-good: #66BB6A;
    --c-critical: #FF5252;
    --c-tooltip-bg: rgba(202, 218, 221, 0.9);

    /* --- LAYOUT: Absolute fill --- */
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;
    justify-content: center;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.5em 0.7em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

/* --- HOVER GLOW --- */
.dscr-panel:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 22px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ===================
   HEADER
   =================== */
.panel-header {
    flex: 0 0 auto;
    z-index: 2;
    line-height: 1.1;
    margin-bottom: 0.3em;
}

.panel-title {
    font-size: 0.7em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

/* ===================
   STATUS BOX
   =================== */
.status-box {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: rgba(255, 255, 255, 0.04);
    border-radius: 0.4em;
    padding: 0.45em 0.6em;
    border: 1px solid rgba(255, 255, 255, 0.08);
    transition: all 0.3s ease;
    flex: 1 1 auto;
    min-height: 0;
    z-index: 2;
    gap: 0.5em;
}

/* Compliant State (Green) */
.status-box.compliant {
    border-color: rgba(102, 187, 106, 0.35);
    background: linear-gradient(90deg, rgba(102, 187, 106, 0.06) 0%, transparent 100%);
}

/* Breach State (Red) */
.status-box.breach {
    border-color: rgba(255, 82, 82, 0.35);
    background: linear-gradient(90deg, rgba(255, 82, 82, 0.06) 0%, transparent 100%);
}

/* ===================
   DSCR MAIN VALUE
   =================== */
.dscr-main {
    display: flex;
    align-items: baseline;
    gap: 0.3em;
}

.dscr-label {
    font-size: 0.55em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
}

.dscr-value {
    font-size: 1.6em;
    font-weight: 700;
    line-height: 1;
}

/* Dynamic colors by state */
.compliant .dscr-value,
.compliant .status-value,
.compliant .status-icon svg {
    color: var(--c-good);
    fill: var(--c-good);
}

.breach .dscr-value,
.breach .status-value,
.breach .status-icon svg {
    color: var(--c-critical);
    fill: var(--c-critical);
}

/* ===================
   STATUS INDICATOR
   =================== */
.status-indicator {
    display: flex;
    align-items: center;
    gap: 0.3em;
}

.status-icon svg {
    width: 1.3em;
    height: 1.3em;
}

/* SVG icon visibility */
.status-icon .icon-compliant,
.status-icon .icon-breach {
    display: none;
}

.compliant .status-icon .icon-compliant {
    display: block;
}

.breach .status-icon .icon-breach {
    display: block;
}

.status-text-group {
    display: flex;
    gap: 0.15em;
    font-size: 0.6em;
    font-weight: 600;
    text-transform: uppercase;
    white-space: nowrap;
}

.status-label {
    color: var(--c-text-muted);
}

/* ===================
   FOOTER
   =================== */
.panel-footer {
    flex: 0 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 0.3em;
    z-index: 2;
    line-height: 1.1;
}

.footer-item {
    font-size: 0.45em;
    font-weight: 500;
    color: var(--c-text-dim);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.highlight {
    color: var(--c-text);
    font-weight: 700;
    margin-left: 0.2em;
}

/* ===================
   TOOLTIP
   =================== */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.4em 0.6em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.dscr-panel:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.55em;
    color: #000;
    font-weight: 600;
    line-height: 1.3;
}

/* ===================
   LOADING SKELETON
   =================== */
.skeleton {
    opacity: 0.5;
    animation: pulse-load 1.5s infinite;
}

@keyframes pulse-load {
    0% {
        opacity: 0.3;
    }

    50% {
        opacity: 0.6;
    }

    100% {
        opacity: 0.3;
    }
}

/* ===================
   BREACH PULSE
   =================== */
.breach .status-icon svg {
    animation: pulse-breach 1.8s infinite;
}

@keyframes pulse-breach {
    0% {
        opacity: 1;
    }

    50% {
        opacity: 0.5;
    }

    100% {
        opacity: 1;
    }
}