/* ════════════════════════════════════════════════════
   Curtailment vs Potential Power — V3 TIMESERIES
   ThingsBoard v4.2.1.1 PE | Timeseries Widget
   ════════════════════════════════════════════════════ */

.pv-curtailment-widget {
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-modal-bg: rgba(5, 20, 50, 0.95);

    position: absolute; width: 100%; height: 100%; margin: 0;
    display: flex; flex-direction: column; align-items: stretch;
    background: var(--c-bg); border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px; padding: 0.35em 0.5em 0.25em; box-sizing: border-box;
    font-family: 'Roboto', sans-serif; color: var(--c-text);
}

.header-section {
    flex: 0 0 auto; display: flex; align-items: center; justify-content: space-between;
    z-index: 2; line-height: 1.1; margin-bottom: 0.1em;
}

.title-container {
    font-size: 0.65em; font-weight: 700; text-transform: uppercase;
    display: flex; align-items: center; gap: 6px;
}

.status-badge { width: 0.35em; height: 0.35em; border-radius: 50%; background: gray; }
.status-badge.live { background: #66BB6A; box-shadow: 0 0 5px rgba(102, 187, 106, 0.6); }
.status-badge.simulated { background: #FFC107; box-shadow: 0 0 5px rgba(255, 193, 7, 0.6); }

.status-text { font-size: 0.38em; font-weight: 600; color: var(--c-text-dim); }

.settings-btn { cursor: pointer; opacity: 0.7; transition: 0.2s; font-size: 0.8em; }
.settings-btn:hover { opacity: 1; transform: rotate(45deg); }

.chart-container { flex: 1 1 0; position: relative; min-height: 0; }
canvas { width: 100% !important; height: 100% !important; }

.custom-tooltip {
    position: absolute; bottom: 0; left: 0; right: 0; padding: 0.4em 0.55em;
    background: rgba(202, 218, 221, 0.92); backdrop-filter: blur(4px);
    transform: translateY(100%); opacity: 0; transition: all 0.3s;
    border-radius: 0 0 8px 8px; font-size: 0.45em; color: #1a1a2e; font-weight: 600;
}
.pv-curtailment-widget:hover .custom-tooltip { opacity: 1; transform: translateY(0); }

/* Settings Modal */
.settings-modal-overlay {
    position: absolute; top: 0; left: 0; right: 0; bottom: 0;
    background: rgba(0,0,0,0.6); backdrop-filter: blur(4px);
    z-index: 100; display: flex; align-items: center; justify-content: center;
}
.settings-modal-content {
    background: var(--c-modal-bg); border: 1px solid var(--c-accent-border);
    padding: 15px; border-radius: 8px; width: 85%; max-width: 300px;
}
.settings-modal-content h3 { margin: 0 0 10px 0; font-size: 14px; text-align: center; }
.setting-group { margin-bottom: 8px; display: flex; flex-direction: column; }
.setting-group label { font-size: 9px; color: var(--c-text-dim); margin-bottom: 3px; }
.setting-group input, .setting-group select { 
    background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2); 
    color: white; padding: 4px; border-radius: 4px; font-size: 11px;
}
.setting-row { display: flex; gap: 10px; }
.settings-modal-actions { display: flex; justify-content: flex-end; gap: 8px; margin-top: 15px; }
.btn-cancel, .btn-save { padding: 5px 10px; border: none; border-radius: 4px; cursor: pointer; font-size: 11px; }
.btn-cancel { background: rgba(255,255,255,0.2); color: white; }
.btn-save { background: var(--c-accent); color: black; font-weight: bold; }
