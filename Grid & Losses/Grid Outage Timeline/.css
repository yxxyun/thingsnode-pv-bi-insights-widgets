/* ════════════════════════════════════════════════════
   Grid Outage Timeline — v2.0
   ThingsBoard v4.3.0 PE | Latest Values
   Horizontal event timeline with severity coloring
   Dynamic scaling — all sizes in em
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & TOKENS
   ───────────────────────────── */
.timeline-card {
    /* Accent — cyan default */
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.7);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.22);

    /* Background */
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);

    /* Text hierarchy */
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;

    /* Semantic */
    --c-good: #66BB6A;
    --c-warning: #FFC107;
    --c-critical: #FF5252;

    /* Event severities */
    --c-evt-high: #E53935;
    --c-evt-high-dark: #B71C1C;
    --c-evt-med: #FB8C00;
    --c-evt-med-dark: #E65100;
    --c-evt-low: #FDD835;
    --c-evt-low-dark: #F9A825;
    --c-evt-maint: #42A5F5;
    --c-evt-maint-dark: #1565C0;

    /* Surfaces */
    --c-track: rgba(255, 255, 255, 0.05);
    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    /* Layout — absolute fill */
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.5em 0.7em 0.4em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

.timeline-card:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 22px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1;
    margin-bottom: 0.15em;
    min-height: 0;
}

.card-title {
    font-size: 0.7em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    min-width: 0;
}

.header-right {
    display: flex;
    align-items: center;
    gap: 0.25em;
    flex-shrink: 0;
    padding-left: 0.4em;
}

/* ─── Status Dot ─── */
.status-dot {
    width: 0.4em;
    height: 0.4em;
    border-radius: 50%;
    background-color: var(--c-text-muted);
    display: inline-block;
    transition: background-color 0.4s ease, box-shadow 0.4s ease;
}

.status-dot.good {
    background-color: var(--c-good);
    box-shadow: 0 0 6px rgba(102, 187, 106, 0.6);
}

.status-dot.warning {
    background-color: var(--c-warning);
    box-shadow: 0 0 6px rgba(255, 193, 7, 0.6);
}

.status-dot.critical {
    background-color: var(--c-critical);
    box-shadow: 0 0 6px rgba(255, 82, 82, 0.6);
    animation: tl-pulse-critical 1.8s infinite;
}

.status-text {
    font-size: 0.42em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
    transition: color 0.3s ease;
}

.status-text.good {
    color: var(--c-good);
}

.status-text.warning {
    color: var(--c-warning);
}

.status-text.critical {
    color: var(--c-critical);
}

.window-label {
    font-size: 0.38em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    background: rgba(255, 255, 255, 0.06);
    padding: 0.15em 0.5em;
    border-radius: 3px;
    white-space: nowrap;
}

/* ─────────────────────────────
   SUMMARY STATS ROW
   ───────────────────────────── */
.summary-row {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    gap: 0.5em;
    margin-bottom: 0.2em;
    z-index: 2;
}

.stat-item {
    display: flex;
    align-items: baseline;
    gap: 0.2em;
}

.stat-label {
    font-size: 0.38em;
    font-weight: 600;
    color: var(--c-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.03em;
}

.stat-value {
    font-size: 0.5em;
    font-weight: 700;
    color: var(--c-accent);
    white-space: nowrap;
}

.stat-sep {
    width: 1px;
    height: 0.7em;
    background: rgba(255, 255, 255, 0.12);
    flex-shrink: 0;
}

/* ─────────────────────────────
   TIMELINE BODY
   ───────────────────────────── */
.timeline-body {
    flex: 1 1 auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    z-index: 2;
    min-height: 0;
    padding-top: 1.2em;
    padding-bottom: 1.0em;
    /* room for below labels */
    /* room for floating labels */
}

/* The horizontal track */
.timeline-track {
    width: 100%;
    height: 1.2em;
    background: var(--c-track);
    border-radius: 0.15em;
    position: relative;
}

/* Individual Event Block */
.timeline-event {
    position: absolute;
    top: 0;
    height: 100%;
    border-radius: 0.15em;
    box-shadow: 0 0.08em 0.2em rgba(0, 0, 0, 0.5);
    cursor: pointer;
    transition: all 0.2s ease;
    border: 1px solid rgba(255, 255, 255, 0.2);
    z-index: 2;
}

.timeline-event:hover {
    transform: translateY(-0.08em);
    z-index: 10;
    box-shadow: 0 0.15em 0.5em rgba(0, 0, 0, 0.6);
    border-color: rgba(255, 255, 255, 0.6);
}

/* Severity colors */
.evt-high {
    background: linear-gradient(180deg, var(--c-evt-high) 0%, var(--c-evt-high-dark) 100%);
}

.evt-med {
    background: linear-gradient(180deg, var(--c-evt-med) 0%, var(--c-evt-med-dark) 100%);
}

.evt-low {
    background: linear-gradient(180deg, var(--c-evt-low) 0%, var(--c-evt-low-dark) 100%);
}

.evt-maint {
    background: linear-gradient(180deg, var(--c-evt-maint) 0%, var(--c-evt-maint-dark) 100%);
}

/* ─── Floating Labels (above by default) ─── */
.event-label-group {
    position: absolute;
    top: -1.1em;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    pointer-events: none;
    z-index: 20;
    white-space: nowrap;
    transition: opacity 0.2s ease;
}

/* Below variant — alternated for close events */
.event-label-group.label-below {
    top: auto;
    bottom: -1.1em;
}

.evt-title {
    font-size: 0.4em;
    font-weight: 700;
    color: #fff;
    text-shadow: 0 0.08em 0.2em rgba(0, 0, 0, 0.9);
    display: block;
    margin-bottom: 0.05em;
}

.evt-sub {
    font-size: 0.35em;
    color: #E0E0E0;
    text-shadow: 0 0.08em 0.2em rgba(0, 0, 0, 0.9);
    display: block;
}

/* ─── Axis ─── */
.timeline-axis {
    display: flex;
    justify-content: space-between;
    margin-top: 1.4em;
    /* clear below-positioned event labels */
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    padding-top: 0.15em;
}

.axis-tick {
    font-size: 0.35em;
    color: var(--c-text-muted);
    position: relative;
    text-align: center;
    min-width: 1.5em;
}

.axis-tick::before {
    content: '';
    position: absolute;
    top: -0.4em;
    left: 50%;
    transform: translateX(-50%);
    width: 1px;
    height: 0.2em;
    background-color: var(--c-text-muted);
}

/* ─── Empty State ─── */
.empty-state {
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.5em;
    color: var(--c-text-muted);
    font-weight: 500;
    padding: 1em 0;
    opacity: 0.6;
}

/* ─────────────────────────────
   EVENT TOOLTIP — JS-driven
   ───────────────────────────── */
.event-tooltip {
    position: absolute;
    bottom: 2.5em;
    left: 0.5em;
    background: rgba(0, 0, 0, 0.92);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    padding: 0.3em 0.45em;
    border-radius: 0.2em;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.2s ease;
    z-index: 50;
    max-width: 12em;
}

.event-tooltip .tt-type {
    font-size: 0.45em;
    font-weight: 700;
    color: #fff;
    margin-bottom: 0.08em;
}

.event-tooltip .tt-date {
    font-size: 0.35em;
    color: #ccc;
}

.event-tooltip .tt-dur {
    font-size: 0.35em;
    color: #ccc;
}

.event-tooltip .tt-energy {
    font-size: 0.38em;
    font-weight: 600;
    color: var(--c-critical);
    margin-top: 0.1em;
}

/* ─────────────────────────────
   CARD TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.35em 0.5em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.timeline-card:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.42em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   ANIMATIONS
   ───────────────────────────── */
@keyframes tl-pulse-critical {
    0% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }

    50% {
        box-shadow: 0 0 10px rgba(255, 82, 82, 0.9);
    }

    100% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }
}