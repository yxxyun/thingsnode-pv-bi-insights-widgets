/* ════════════════════════════════════════════════════
   Insurance Claimable Events Summary — v2.0
   ThingsBoard v4.3.0 PE | Latest Values
   Table-style claims with status pills
   Dynamic scaling — all sizes in em
   ════════════════════════════════════════════════════ */

/* ─────────────────────────────
   ROOT & TOKENS
   ───────────────────────────── */
.claims-card {
    /* Accent */
    --c-accent: #06F5FF;
    --c-accent-glow: rgba(6, 245, 255, 0.12);
    --c-accent-border: rgba(6, 245, 255, 0.4);
    --c-accent-hover: rgba(6, 245, 255, 0.7);
    --c-accent-glow-hover: rgba(6, 245, 255, 0.22);

    /* Background */
    --c-bg: linear-gradient(180deg, rgba(2, 10, 67, 0.95) 0%, rgba(5, 20, 50, 0.95) 100%);

    /* Text hierarchy */
    --c-text: #FFFFFF;
    --c-text-dim: #B0BEC5;
    --c-text-muted: #90A4AE;

    /* Semantic */
    --c-good: #66BB6A;
    --c-warning: #FFC107;
    --c-critical: #FF5252;

    /* Status pill */
    --c-pending-bg: rgba(255, 202, 40, 0.15);
    --c-pending: #FFCA28;
    --c-approved-bg: rgba(102, 187, 106, 0.15);
    --c-approved: #66BB6A;
    --c-rejected-bg: rgba(255, 82, 82, 0.15);
    --c-rejected: #FF5252;

    /* Surfaces */
    --c-row-hover: rgba(255, 255, 255, 0.04);
    --c-header-bg: rgba(255, 255, 255, 0.05);
    --c-border: rgba(255, 255, 255, 0.08);
    --c-tooltip-bg: rgba(202, 218, 221, 0.92);

    /* Layout — absolute fill */
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    margin: 0;

    display: flex;
    flex-direction: column;
    align-items: stretch;

    background: var(--c-bg);
    border: 1px solid var(--c-accent-border);
    box-shadow: 0 0 12px var(--c-accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.4);
    border-radius: 8px;
    padding: 0.5em 0.6em 0.4em;
    box-sizing: border-box;
    overflow: hidden;
    z-index: 1;

    font-family: 'Roboto', sans-serif;
    color: var(--c-text);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

.claims-card:hover {
    border-color: var(--c-accent-hover);
    box-shadow: 0 0 22px var(--c-accent-glow-hover), inset 0 0 8px rgba(0, 0, 0, 0.2);
}

/* ─────────────────────────────
   HEADER
   ───────────────────────────── */
.card-header {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 2;
    line-height: 1;
    margin-bottom: 0.15em;
    min-height: 0;
}

.card-title {
    font-size: 0.85em;
    font-weight: 700;
    color: var(--c-text);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    min-width: 0;
}

.header-right {
    display: flex;
    align-items: center;
    gap: 0.25em;
    flex-shrink: 0;
    padding-left: 0.4em;
}

/* ─── Status Dot ─── */
.status-dot {
    width: 0.4em;
    height: 0.4em;
    border-radius: 50%;
    background-color: var(--c-text-muted);
    display: inline-block;
    transition: background-color 0.4s ease, box-shadow 0.4s ease;
}

.status-dot.good {
    background-color: var(--c-good);
    box-shadow: 0 0 6px rgba(102, 187, 106, 0.6);
}

.status-dot.warning {
    background-color: var(--c-warning);
    box-shadow: 0 0 6px rgba(255, 193, 7, 0.6);
}

.status-dot.critical {
    background-color: var(--c-critical);
    box-shadow: 0 0 6px rgba(255, 82, 82, 0.6);
    animation: claims-pulse-crit 1.8s infinite;
}

.status-text {
    font-size: 0.52em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    white-space: nowrap;
    transition: color 0.3s ease;
}

.status-text.good {
    color: var(--c-good);
}

.status-text.warning {
    color: var(--c-warning);
}

.status-text.critical {
    color: var(--c-critical);
}

/* ─────────────────────────────
   SUMMARY STATS ROW
   ───────────────────────────── */
.summary-row {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    gap: 0.5em;
    margin-bottom: 0.2em;
    z-index: 2;
}

.stat-item {
    display: flex;
    align-items: baseline;
    gap: 0.15em;
}

.stat-label {
    font-size: 0.45em;
    font-weight: 600;
    color: var(--c-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.03em;
}

.stat-value {
    font-size: 0.58em;
    font-weight: 700;
    color: var(--c-accent);
    white-space: nowrap;
}

.stat-good {
    color: var(--c-good);
}

.stat-warn {
    color: var(--c-warning);
}

.stat-bad {
    color: var(--c-critical);
}

.stat-sep {
    width: 1px;
    height: 0.6em;
    background: rgba(255, 255, 255, 0.1);
    flex-shrink: 0;
}

/* ─────────────────────────────
   TABLE HEADER
   ───────────────────────────── */
.table-header {
    flex: 0 0 auto;
    display: flex;
    background: var(--c-header-bg);
    border-radius: 0.15em 0.15em 0 0;
    padding: 0.25em 0.35em;
    border-bottom: 1px solid var(--c-border);
    z-index: 2;
}

.table-header .col {
    font-size: 0.5em;
    font-weight: 700;
    color: var(--c-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.03em;
}

/* Column widths */
.col {
    display: flex;
    align-items: center;
}

.col-date {
    width: 15%;
}

.col-event {
    width: 30%;
    flex: 1;
    min-width: 0;
}

.col-lost {
    width: 15%;
    justify-content: flex-end;
    text-align: right;
}

.col-amount {
    width: 20%;
    justify-content: flex-end;
    text-align: right;
}

.col-status {
    width: 20%;
    justify-content: center;
}

/* ─────────────────────────────
   TABLE BODY
   ───────────────────────────── */
.table-body {
    flex: 1 1 auto;
    overflow-y: auto;
    min-height: 0;
    scrollbar-width: thin;
    scrollbar-color: rgba(255, 255, 255, 0.15) transparent;
}

.table-body::-webkit-scrollbar {
    width: 4px;
}

.table-body::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.15);
    border-radius: 2px;
}

.table-row {
    display: flex;
    padding: 0.28em 0.35em;
    border-bottom: 1px solid var(--c-border);
    transition: background 0.2s ease;
    align-items: center;
}

.table-row:hover {
    background: var(--c-row-hover);
}

.val-date {
    font-size: 0.5em;
    color: var(--c-text-dim);
}

.val-event {
    font-size: 0.52em;
    font-weight: 500;
    color: #fff;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.val-lost {
    font-size: 0.5em;
    font-family: 'Roboto Mono', monospace;
    color: var(--c-text-dim);
}

.val-amount {
    font-size: 0.52em;
    font-family: 'Roboto Mono', monospace;
    font-weight: 700;
    color: #fff;
}

/* ─── Status Pills ─── */
.status-pill {
    padding: 0.12em 0.4em;
    border-radius: 0.6em;
    font-size: 0.42em;
    font-weight: 700;
    text-transform: uppercase;
    text-align: center;
    min-width: 3em;
    display: inline-block;
}

.status-pill.pending {
    background: var(--c-pending-bg);
    color: var(--c-pending);
    border: 1px solid var(--c-pending);
}

.status-pill.approved {
    background: var(--c-approved-bg);
    color: var(--c-approved);
    border: 1px solid var(--c-approved);
}

.status-pill.rejected {
    background: var(--c-rejected-bg);
    color: var(--c-rejected);
    border: 1px solid var(--c-rejected);
    opacity: 0.65;
}

/* ─── Empty State ─── */
.empty-state {
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.55em;
    color: var(--c-text-muted);
    font-weight: 500;
    padding: 1.5em 0;
    opacity: 0.6;
    font-style: italic;
}

/* ─────────────────────────────
   FOOTER
   ───────────────────────────── */
.card-footer {
    flex: 0 0 auto;
    margin-top: auto;
    padding-top: 0.25em;
    display: flex;
    justify-content: flex-end;
    align-items: baseline;
    gap: 0.3em;
    border-top: 1px solid var(--c-border);
    z-index: 2;
}

.footer-label {
    font-size: 0.52em;
    font-weight: 600;
    color: var(--c-text-dim);
    text-transform: uppercase;
}

.footer-value {
    font-size: 0.9em;
    font-weight: 700;
    color: #fff;
    text-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
}

/* ─────────────────────────────
   CARD TOOLTIP — Glassmorphism
   ───────────────────────────── */
.tooltip-container {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.3em 0.5em;
    background-color: var(--c-tooltip-bg);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    transform: translateY(100%);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 10;
    pointer-events: none;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.claims-card:hover .tooltip-container {
    opacity: 1;
    transform: translateY(0);
}

.tooltip-content {
    font-size: 0.4em;
    color: #1a1a2e;
    font-weight: 600;
    line-height: 1.3;
}

/* ─────────────────────────────
   ANIMATIONS
   ───────────────────────────── */
@keyframes claims-pulse-crit {
    0% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }

    50% {
        box-shadow: 0 0 10px rgba(255, 82, 82, 0.9);
    }

    100% {
        box-shadow: 0 0 4px rgba(255, 82, 82, 0.6);
    }
}